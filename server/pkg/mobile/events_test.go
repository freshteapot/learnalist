package mobile_test

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("Testing Events", func() {
	It("TODO", func() {
		a := "TODO"
		Expect(a).To(Equal(a))
	})

	It("Remove mobile device token from db", func() {

	})
})
