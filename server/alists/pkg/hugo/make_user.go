package hugo

// WriteListsByUser
func (h HugoHelper) WriteListsByUser(userUUID string, lists []string) {
	/*
		uuid := aList.Uuid
		content, _ := json.Marshal(aList)
		path := fmt.Sprintf("%s/%s.json", h.DataDirectory, uuid)
		err := ioutil.WriteFile(path, content, 0644)
		if err != nil {
			fmt.Println(err)
		}

		h.RegisterCronJob()
	*/
}
